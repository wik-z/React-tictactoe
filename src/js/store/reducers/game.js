const initialState = {
    board: Array(9),
    playerOne: null,
    playerTwo: null,
    resolution: null,
    scores: [],
};

export default function game(state = initialState, action) {
    return state;
}